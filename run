#Okay so I did not know how to do this with packages so I used perplexity to write this for me - thanks ai

#!/bin/bash
cd "$(dirname "$0")/src"  # Go to src/ directory
rm -f model/*.class simulation/*.class truck/*.class #Remove all of the class files
javac -d ../build model/*.java simulation/*.java
cd ../build
java simulation.Demo
